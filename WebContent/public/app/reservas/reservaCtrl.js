/**
 *
 * @Author 
 */


(function(window, angular) {
	/* llamado del modulo */
	angular
		.module("Prestamos").controller('reservaCtrl',reservaCtrl);
	
	/*Inyeccion de las dependencias para el controlador*/
	reservaCtrl.$inject = [];
	
	
	function reservaCtrl() {
		
		
		
		
	}
})(window, window.angular);